apiVersion: apps/v1
kind: Deployment
metadata:
  name: bind9-git
  namespace: dns
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: bind9-git
  template:
    metadata:
      labels:
        app: bind9-git
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      nodeSelector:
        kubernetes.io/hostname: "k8s-node01"
      securityContext:  # 修正点1：正确的位置
        capabilities:
          add: ["NET_BIND_SERVICE"]
        runAsUser: 0
      containers:
      - name: bind9
        image: docker.1ms.run/ubuntu/bind9:latest
        command: ["/bin/sh", "-c"]
        args:
        - |
          # 安装 git
          apt-get update && apt-get install -y git
          
          git config --global user.email "p4s2wd@gmail.com"
          git config --global user.name "p4s2wd"
          git config --global --add safe.directory /etc/bind
          git config --global https.proxy https://192.168.2.135:7890
          git clone https://github.com/p4s2wd/dns-config.git /etc/bind
          
          # 设置权限
          chown -R 100:101 /etc/bind
          find /etc/bind -type d -exec chmod 755 {} \;
          find /etc/bind -type f -exec chmod 644 {} \;
          
          cd /etc/bind
          /monitor.sh &
          exec su -s /bin/sh -c "exec named -g -c /etc/bind/config/named.conf" bind
        ports:
        - containerPort: 53
          protocol: UDP
        - containerPort: 53
          protocol: TCP
        - containerPort: 953
          protocol: TCP
        securityContext:  # 修正点2：容器级安全上下文（可选）
          capabilities:
            add: ["NET_BIND_SERVICE"]
          runAsUser: 100  # 建议改用非root用户
